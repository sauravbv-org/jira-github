name: Auto approve PRs
on: pull_request_target

jobs:
  auto-approve:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    if: github.actor == 'sauravbv'
    steps:
      - uses: hmarr/auto-approve-action@v3
